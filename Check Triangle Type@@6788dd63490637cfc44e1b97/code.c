#include <stdio.h>
int main() {
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if(a==a==a){
        printf("Equilateral");
    }
    else {
        printf("Scalene");
    }
    return 0;
}