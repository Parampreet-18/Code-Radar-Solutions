#include <stdio.h>
int main() {
    int a,b,c,formul,formula;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    formul == c*c;
    formula == a*a +b*b;

    if(formul == formula){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    return 0;
}